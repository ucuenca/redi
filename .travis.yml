language: java

jdk:
- oraclejdk7
- oraclejdk8

install: true

cache:
  directories:
  - $HOME/.m2

before_install:
  - echo "MAVEN_OPTS='-Xmx2g'" > ~/.mavenrc
  - npm install -g npm@next 

script: mvn clean install
